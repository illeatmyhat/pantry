# ja-JP ground truth — HUMAN-written, over the generated baseline.
#
# Keyed by fdc_id. Field-level: state only what you verified (name,
# aliases, errand, notes); everything else flows through from the machine
# baseline. `basis` is REQUIRED — say why the machine was wrong. This file
# survives every regeneration and wins over ANY machine output.
#
# Agents never write here: a model fixing machine output edits the
# generated baseline (results JSONL) directly and re-validates.
#
# Example (delete when real entries exist):
#
# 168229:
#   name: 豚肉、生鮮（非加塩）、もも（ハム）、すね半分、赤身のみ、加熱調理、ロースト
#   basis: '"fresh" renders as 生鮮（非加塩）, never 生 (glossary decision 2026-06-12)'
